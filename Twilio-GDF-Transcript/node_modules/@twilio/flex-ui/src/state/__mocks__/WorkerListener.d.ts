/// <reference types="jest" />
import { IWorker } from "../../models";
import { SyncClient } from "twilio-sync";
import { InsightsTask } from "../../models/InsightsModel";
export declare const WorkerListener: jest.Mock<{
    state: {
        worker: IWorker<any>;
        tasks: InsightsTask[];
    };
    unInit: jest.Mock<any, any>;
    updateWorker: jest.Mock<any, any>;
}, [worker: IWorker<any>, insightsClient: SyncClient, onTasksUpdated: (item: any) => void, taskFilter: string]>;
