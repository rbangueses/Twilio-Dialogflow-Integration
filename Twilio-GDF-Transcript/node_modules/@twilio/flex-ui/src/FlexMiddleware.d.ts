import { GenericStoreEnhancer } from "redux";
import { History } from "history";
import { HistoryType, HistoryParams } from "./history";
/**
 * Applies Flex middleware to redux store
 * @example
 * const myReduxStore = createStore(
 *  reducers,
 *  compose(
 *    applyFlexMiddleware()
 *  )
 * );
 *
 * @example
 * import createMemoryHistory from "history/createMemoryHistory";
 * const history = createMemoryHistory();
 * const myReduxStore = createStore(
 *  reducers,
 *  compose(
 *    applyFlexMiddleware(history)
 *  )
 * );
 *
 * @param {History | HistoryType | HistoryParams} [arg] instance of History, type of a history (memory, browser) or parameters for getHistory() function
 * @category Core / State
 * @method applyFlexMiddleware
 * @memberof Store
 */
export declare const applyFlexMiddleware: (arg?: History | HistoryType | HistoryParams, enableReduxLogging?: boolean) => GenericStoreEnhancer;
