import { AbstractTelemetryEventsDelegate } from "@twilio/flex-ui-core";
export interface UIInteractionEvent {
    name: string;
    status: "start" | "complete";
    category?: string;
    details?: Record<string, string>;
    duration?: number;
}
interface FormattedUIInteractionEvent {
    interaction_name: string;
    interaction_status: "start" | "complete";
    interaction_category?: string;
    interaction_duration_ms?: number;
    interaction_details_map?: Record<string, string>;
}
export declare class UIInteractionEventsDelegate extends AbstractTelemetryEventsDelegate<FormattedUIInteractionEvent> {
    getSchemaName(): string;
    getEventName(): string;
    format({ name, status, category, details, duration }: UIInteractionEvent): FormattedUIInteractionEvent;
}
export {};
