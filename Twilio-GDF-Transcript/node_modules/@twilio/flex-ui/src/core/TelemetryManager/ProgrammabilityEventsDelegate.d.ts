import { AbstractTelemetryEventsDelegate } from "@twilio/flex-ui-core";
import { ProgrammabilityEvent, ProgrammabilityEventCallType } from "./ProgrammabilityEvents.definitions";
interface FormattedProgrammabilityEvent {
    plugin: string;
    plugin_version?: string;
    call_type: ProgrammabilityEventCallType;
    name: string;
    command: string;
    listenee?: string;
    options_map?: Record<string, string>;
}
export declare class ProgrammabilityEventsDelegate extends AbstractTelemetryEventsDelegate<FormattedProgrammabilityEvent> {
    currentPlugin: string;
    currentPluginVersion: string;
    events: FormattedProgrammabilityEvent[];
    currentOptions: Record<string, unknown>;
    private eventGroupEnabled;
    getSchemaName(): string;
    getEventName(): string;
    report(event: FormattedProgrammabilityEvent): Promise<void>;
    stopProgrammabilityGrouping(): void;
    format(event: ProgrammabilityEvent): FormattedProgrammabilityEvent;
}
export {};
