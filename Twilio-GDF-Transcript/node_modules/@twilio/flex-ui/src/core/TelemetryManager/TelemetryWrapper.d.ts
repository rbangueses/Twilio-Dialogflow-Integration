import * as React from "react";
interface TelemetryWrapperProps {
    name: string;
    data?: any;
    category?: string;
    details?: Record<string, string>;
}
/**
 * Watches `data` prop and reports telemetry when it changes
 */
export declare class TelemetryWrapper extends React.Component<TelemetryWrapperProps> {
    private startTime?;
    report(details: Record<string, string>, duration?: number): void;
    componentDidMount(): void;
    componentDidUpdate(prevProps: TelemetryWrapperProps): void;
    componentWillUnmount(): void;
    render(): React.ReactNode;
}
export {};
