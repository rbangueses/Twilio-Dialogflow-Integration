import { AbstractTelemetryEventsDelegate } from "@twilio/flex-ui-core";
import { TelemetryErrorEvent } from "@twilio/flex-sdk";
import { FlexError } from "../../FlexError";
export interface ErrorEvent extends TelemetryErrorEvent {
}
export declare class ErrorEventDelegate extends AbstractTelemetryEventsDelegate<ErrorEvent> {
    private errorMap;
    private getErrorSeverity;
    getSchemaName(): string;
    getEventName(): string;
    report(event: TelemetryErrorEvent): Promise<void>;
    format(error: FlexError): TelemetryErrorEvent;
}
