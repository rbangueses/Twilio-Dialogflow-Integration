import { PluginDefinition } from "./PluginManager";
/**
 * Returns true if the plugin proxy should be enabled.
 *
 * @private
 * @returns {boolean}
 */
export declare const enablePluginProxy: () => boolean;
/**
 * Returns true if the plugin activity console logging should be enabled.
 *
 * @private
 * @returns {boolean}
 */
export declare const enablePluginActivityConsoleLogging: () => boolean;
/**
 * Gets the global Proxy for this plugin if it's enabled.
 *
 * @private
 * @param {name} string
 * @returns {Proxy}
 */
export declare const getPluginProxy: (name: string) => typeof import("..");
/**
 * Initializes a Proxy for a plugin and adds it to the global FlexProxy object.
 *
 * @private
 * @param {PluginDefinition} plugin
 * @returns {void}
 */
export declare const setupPluginProxy: (plugin: PluginDefinition) => void;
/**
 * Initializes a Proxy for a plugin.
 *
 * @private
 * @param {PluginDefinition} plugin
 * @returns {Proxy}
 */
export declare const createPluginProxy: (plugin: PluginDefinition) => typeof import("..");
