import { AxiosInstance } from "axios";
export interface IdentityClient {
    axios: AxiosInstance;
    loading: boolean;
    isLoggedIn: boolean;
    insightsToken?: string;
    user?: {
        workspace_id?: string;
        account_id?: string;
        name?: string;
        email?: string;
    };
    hasPermission: (permission: string) => boolean;
    ssoLogin(args: {
        identity: string;
        token: string;
        account_sid: string;
    }): Promise<void>;
    getServiceSsoUrl(targetService: string, targetUrl: string, params?: object): Promise<string>;
    getWorkspaceEncryptedClaims(): Promise<string | undefined>;
    getSSOProvider(): Promise<string>;
    logout(): Promise<void>;
}
export declare type IdentityClientOptions = {
    url: string;
    baseUrl: string;
    onPropertyUpdated?: any;
    onLoggedInChanged?: any;
    ssoLoginUrl?: string;
};
