import { AxiosInstance } from "axios";
import { Manager } from "../../Manager";
import { IdentityClient } from "./Identity.definitions";
export declare class YticaIdentityClient implements IdentityClient {
    axios: AxiosInstance;
    private yticaClient;
    private urlObj?;
    get isLoggedIn(): boolean;
    get loading(): boolean;
    get user(): IdentityClient["user"];
    constructor(manager: Manager);
    hasPermission(permission: string): boolean;
    ssoLogin(args: {
        identity: string;
        token: string;
        account_sid: string;
    }): Promise<void>;
    getServiceSsoUrl(targetService: string, targetUrl: string, params?: object): Promise<string>;
    logout(): Promise<void>;
    private initDashboardServiceUrl;
    getWorkspaceEncryptedClaims(): Promise<string>;
    getSSOProvider(): Promise<string>;
}
