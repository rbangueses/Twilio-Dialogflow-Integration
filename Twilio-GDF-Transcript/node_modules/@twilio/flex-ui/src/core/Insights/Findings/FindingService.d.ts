export declare class FindingService {
    private static instance;
    private insightsService;
    private playerConfig;
    private dispatch;
    constructor();
    static getInstance(): FindingService;
    canViewFindings(): boolean;
    fetchFindings(segmentId: string): Promise<void>;
    fetchConversationFindings(segmentIds: string[]): Promise<void>;
}
