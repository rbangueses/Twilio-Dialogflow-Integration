import { IAssessment, IComment } from "./Questionnaire.models";
export declare class QuestionnaireService {
    private static instance;
    private insightsService;
    private assessmentsApiUrl;
    private dispatch;
    constructor();
    static getInstance(): QuestionnaireService;
    fetchQuestionnaires(): Promise<void>;
    fetchComments(agentId: string): Promise<void>;
    fetchAssessment(segmentId: string): Promise<void>;
    fetchSettings(): Promise<void>;
    submitAssessment(segmentId: string, assessment: IAssessment): Promise<void>;
    submitComment(segmentId: string, comment: IComment): Promise<IComment>;
}
