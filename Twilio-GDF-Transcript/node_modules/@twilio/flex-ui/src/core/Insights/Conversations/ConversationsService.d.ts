export declare class ConversationsService {
    private static instance;
    private insightsService;
    private playerConfig;
    private dispatch;
    constructor();
    static getInstance(): ConversationsService;
    refreshPresignedRecordingUrl(segmentId: string): Promise<void>;
    fetchSegment(segmentId: string): Promise<void>;
}
