import * as React from "react";
import { IdentityClient } from "../../../core/Insights/Identity/Identity.definitions";
import { YticaAppName } from "./YticaApp";
interface InsightsViewProps {
    getUrl: (client: IdentityClient) => Promise<string>;
    isActive: boolean;
    name: YticaAppName;
    onMessage?: (e: MessageEvent) => void;
}
interface InsightsViewState {
    url: string;
    urlStatus: UrlStatus;
}
declare enum UrlStatus {
    Fetching = 0,
    Fetched = 1,
    Error = 2
}
export default class InsightsView extends React.Component<InsightsViewProps, InsightsViewState> {
    hasFetched: boolean;
    state: InsightsViewState;
    static defaultProps: {
        isActive: boolean;
    };
    private yticaRef;
    private getUrl;
    get iframeRef(): HTMLIFrameElement | null;
    render(): JSX.Element;
}
export {};
