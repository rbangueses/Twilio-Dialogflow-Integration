import * as React from "react";
import { Player as TwilioPlayer } from "@twilio/player";
interface VoiceContentProps {
    segmentId: string;
    onSegmentSelect: (sid: string) => void;
}
interface VoiceContentState {
    playerRef: null | TwilioPlayer;
}
export declare class VoiceContent extends React.Component<VoiceContentProps, VoiceContentState> {
    constructor(props: VoiceContentProps);
    private refFn;
    private handleSegmentSelect;
    render(): JSX.Element;
}
export {};
