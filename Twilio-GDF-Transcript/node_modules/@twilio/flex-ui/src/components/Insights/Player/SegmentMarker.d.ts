import * as React from "react";
import { ThemeProps } from "../../theme";
export interface SegmentMarkerProps extends ThemeProps {
    mark: {
        segmentId: string;
        agentName: string;
        offset: number;
        segmentStart: boolean;
    };
    onClick: (segmentId: string) => void;
}
export declare class SegmentMarker extends React.PureComponent<SegmentMarkerProps> {
    handleClick: (e: React.MouseEvent<HTMLDivElement>) => void;
    render(): JSX.Element;
}
