/// <reference types="lodash" />
import * as React from "react";
import { Player } from "@twilio/player";
import throttle = require("lodash.throttle");
interface CurrentTimeProps {
    player: Player;
}
interface CurrentTimeState {
    time: string;
}
export declare class CurrentTime extends React.Component<CurrentTimeProps, CurrentTimeState> {
    throttledHandleCurrentTimeChange: ReturnType<typeof throttle>;
    state: {
        time: string;
    };
    subscribePlayer: (player: Player) => void;
    unsubscribePlayer: (player: Player) => void;
    componentDidUpdate(prevProps: CurrentTimeProps): void;
    componentDidMount(): void;
    componentWillUnmount(): void;
    handleCurrentTimeChange: () => void;
    render(): JSX.Element;
}
export {};
