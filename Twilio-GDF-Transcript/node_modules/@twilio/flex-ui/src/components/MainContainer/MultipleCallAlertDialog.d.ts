import * as React from "react";
import { ITask } from "../../models/CommonModels";
export declare const MultipleCallAlertDialog: import("react-redux").ComponentClass<Pick<{
    tasks: Array<ITask>;
}, never>> & {
    WrappedComponent: React.ComponentType<{
        tasks: Array<ITask>;
    }>;
};
