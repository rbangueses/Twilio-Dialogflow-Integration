/// <reference types="node" />
import { EventEmitter } from "events";
export declare enum ChatEvent {
    chatInputFocus = 0,
    chatInputBlur = 1,
    chatInputRowsChanged = 2,
    chatMessagePosted = 3,
    chatMediaMessagePosted = 4,
    chatMediaDownloaded = 5
}
export declare class ChatEventEmitter extends EventEmitter {
    emitInputFocus(channelSid: string): void;
    emitInputBlur(channelSid: string): void;
    emitInputRowsChanged(channelSid: string): void;
    emitMessagePosted(channelSid: string, messageBody: string): void;
    emitMediaMessagePosted(channelSid: string, messageBody: File): void;
    emitMediaDownloaded(channelSid: string): void;
}
