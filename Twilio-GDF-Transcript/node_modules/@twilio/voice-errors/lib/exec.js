const childProcess = require('child_process');

async function exec(cmd) {
  return new Promise((resolve, reject) => {
    childProcess.exec(cmd, error => error ? reject(error) : resolve());
  });
}

module.exports = exec;
